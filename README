ЗАДАЧА
Требуется написать сервис, который будет обрабатывать входящие запросы по TCP в формате MessagePack, сохранять в памяти значения полей "domain" и "ip"
в формате key:value с TTL 10 секунд, выводя при этом каждую секунду полный список хранимых данных.
Форматы: domain - string, ip - uint32.
По умолчанию TCP-сервер слушает на 0.0.0.0:6000. Но эти параметры можно изменить с помощью задания следующих опций при старте:
-tcpport=8080 - порт TCP-сервера;
-tcpaddr=127.0.0.1 - адрес TCP-сервера;

РЕШЕНИЕ
Сервер - tcp-client.go
Клиент - tcp-client.go
Вспомогательные модули в каталоге lib: ttlmap.go (ttl-карта), mpmessage.go (маршалинг в MessagePack).

!!!Дополнительные зависимости!!!
Внешний пакет для MessagePack. Перед запуском клиента и сервера для установки пакета запустить: "go get -u github.com/vmihailenco/msgpack"

Запуск сервера:
go run tcp-server.go -tcpaddr=127.0.0.1 -tcpport=8080

Реализован tcp-клиент отправляющий данные каждую секунду
Запуск клиента:
go run tcp-client.go -tcpaddr=127.0.0.1 -tcpport=8080